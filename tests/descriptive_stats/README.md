We assume that you have install [Aphonenia](https://github.com/b-k/apophenia) and its dependencies (the GNU Scientific Library and SQLite3)

    sudo apt-get install make gcc libgsl0-dev libsqlite3-dev 

